# 首先检查URB是否对应
使用root用户，执行wireshark抓去usbmon0的数据包，过滤条件为
```
(usb.dst=="1.11.0") || (usb.src=="1.11.0")||(usb.dst=="1.11.1") || (usb.src=="1.11.1")||(usb.dst=="1.11.2") || (usb.src=="1.11.2")
```
1为总线，11为设备号，0、1、2为端点号。

